Student(s) name: Minha kwon

Title: Transformer Attention 

Summary : The transformer architecture is the very important structure for the current Language Models. Especially, it's a foundation for LLMs. Transformer is also used for code-specific models, like BERT or T5. Transformers use self-attention. It is for capturing relationships between a token and other tokens. So it makes the input sequence to look at each other, not just deal with them sequencly. So the model can capture long distance relationships of the tokens. 
I wanna research the attention mechanism. And conduct a simple experiment on how it effect the result if attention mechanism is changed. The model's task will be very simple task with simple structure, and I wanna process a code sequence as input. Maybe the changed part of attention mechanism will be positional encoding. 
